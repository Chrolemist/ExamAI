// Nodes panels module: placeholder to host node-specific floating panels (chat, internet, etc.)
(function(){
  // This module can progressively take over from legacy panels.js for node panels.
  // Export a minimal hook so other scripts can check availability.
  window.PanelsNodes = {
    version: '0.1.0'
  };
})();
