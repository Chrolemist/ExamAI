name: CI (disabled)

on:
  workflow_dispatch:

permissions: read-all

# This workflow is effectively disabled (manual only) and performs no checks.
jobs:
  noop:
    name: No-op
    runs-on: ubuntu-latest
    steps:
      - name: Print notice
        run: echo "CI temporarily disabled. Run locally while refactoring."
